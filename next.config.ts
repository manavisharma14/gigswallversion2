/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // No need for `experimental.appDir` — it's enabled by default in App Router
};

module.exports = nextConfig;
